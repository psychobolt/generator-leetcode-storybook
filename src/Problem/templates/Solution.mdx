import { Meta, Story, Canvas } from '@storybook/addon-docs';

import code from '<%= solutionPath %>?raw';

<Meta title="<%= titlePath %>" />

# Solution

See source file `<%= solutionPath %>`

<Canvas withSource="open">
  <Story name="Source" parameters={{ docs: { source: { code } } }}>
    <div>Case execution is unsupported for this language. Please test the solution in your local dev environment.</div>
  </Story>
</Canvas>